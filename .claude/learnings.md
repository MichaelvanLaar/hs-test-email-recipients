[cc-config-init] Stack undecided at init time — no PostToolUse formatter hook added; add one once a bundler/formatter is chosen.
[cc-config-init] chrome.storage.local is intentional (not sync) — recipient sets must never leave the device.
[cc-config-init] Target is Manifest V3; reject any MV2-only patterns or APIs.
